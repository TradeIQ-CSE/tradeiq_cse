// Re-exported for existing call sites (the test harness in particular).
// The actual theme configs now live in antd-theme.ts, generated from the
// canonical tokens in tokens.ts — see that file's comment for why.
export { darkAntdTheme as darkTheme, lightAntdTheme as lightTheme } from './antd-theme';
